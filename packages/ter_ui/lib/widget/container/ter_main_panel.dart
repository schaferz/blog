import 'package:flutter/widgets.dart';

/// Felület központi panelje.
class TerMainPanel extends StatelessWidget {
  const TerMainPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
